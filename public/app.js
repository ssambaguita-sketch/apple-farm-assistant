let board = [], filings = [], meta = {};
const $ = (id) => document.getElementById(id);
const safe = (v) => String(v ?? '').replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const won = (v) => { const n=Number(v); if(!Number.isFinite(n)) return '-'; if(n>=1e12) return (n/1e12).toFixed(1)+'조'; if(n>=1e8) return (n/1e8).toFixed(1)+'억'; return Math.round(n).toLocaleString('ko-KR'); };
const pct = (v) => { const n=Number(v); if(!Number.isFinite(n)) return '-'; return n.toFixed(1)+'%'; };
const price = (v) => { const n=Number(v); return Number.isFinite(n)?Math.round(n).toLocaleString('ko-KR'):'-'; };
function dartUrl(no){ return no ? 'https://dart.fss.or.kr/dsaf001/main.do?rcpNo=' + encodeURIComponent(no) : ''; }
async function fetchJsonCompat(url){ const r=await fetch(url,{cache:'no-store'}); if(!r.ok) throw new Error(url.split('?')[0]+' HTTP '+r.status); let t=await r.text(); t=t.replace(/:\s*NaN(?=\s*[,}\]])/g,':null').replace(/:\s*Infinity(?=\s*[,}\]])/g,':null').replace(/:\s*-Infinity(?=\s*[,}\]])/g,':null'); return JSON.parse(t); }
function useText(r){ const parts=[]; if(Number(r.rights_use_facility_krw)>0) parts.push('시설 '+won(r.rights_use_facility_krw)); if(Number(r.rights_use_operating_krw)>0) parts.push('운영 '+won(r.rights_use_operating_krw)); if(Number(r.rights_use_debt_krw)>0) parts.push('채무 '+won(r.rights_use_debt_krw)); if(Number(r.rights_use_acquisition_krw)>0) parts.push('취득 '+won(r.rights_use_acquisition_krw)); if(Number(r.cb_use_facility_krw)>0) parts.push('CB시설 '+won(r.cb_use_facility_krw)); if(Number(r.cb_use_operating_krw)>0) parts.push('CB운영 '+won(r.cb_use_operating_krw)); if(Number(r.cb_use_debt_krw)>0) parts.push('CB채무 '+won(r.cb_use_debt_krw)); if(Number(r.cb_use_acquisition_krw)>0) parts.push('CB취득 '+won(r.cb_use_acquisition_krw)); return parts.length?parts.join('<br>'):'-'; }
async function load(){ $('loadState').textContent='데이터 불러오는 중...'; try { const [data,f]=await Promise.all([fetchJsonCompat('./data.json?ts='+Date.now()),fetchJsonCompat('./filings.json?ts='+Date.now())]); meta=data; board=Array.isArray(meta.board)?meta.board:[]; filings=Array.isArray(f.filings)?f.filings:[]; const s=meta.summary||{},d=meta.discovery||{}; $('auto').textContent=d.auto_discovered??(meta.watchlist||[]).filter(x=>x.source==='auto').length; $('total').textContent=board.length; $('watch').textContent=board.filter(x=>x.state==='WATCH').length; $('research').textContent=board.filter(x=>x.state==='RESEARCH').length; $('avoid').textContent=board.filter(x=>x.state==='AVOID').length; $('termsVerified').textContent=board.filter(x=>x.terms_verified===true||String(x.terms_verified).toLowerCase()==='true').length; $('freshness').textContent='시스템 산출일 '+(s.as_of||'-')+' · 최신 시장가격일 '+(s.price_date_max||board.map(x=>x.price_date).filter(Boolean).sort().slice(-1)[0]||'-'); $('loadState').textContent='정상 로드 · '+board.length+'개 종목'; $('loadState').className='ok'; render(); renderFilings(); } catch(e){ $('loadState').textContent='데이터 로드 실패: '+e.message; $('loadState').className='err'; $('body').innerHTML='<tr><td colspan="14">'+safe(e.message)+'</td></tr>'; } }
function filtered(){ const q=$('q').value.trim().toLowerCase(),st=$('state').value; return board.filter(r=>(!q||String(r.company||'').toLowerCase().includes(q)||String(r.ticker||'').includes(q))&&(!st||r.state===st)).sort((a,b)=>Number(b.market_setup_score??b.opportunity_score??0)-Number(a.market_setup_score??a.opportunity_score??0)||Number(a.risk_score??0)-Number(b.risk_score??0)); }
function render(){ const rows=filtered(); $('body').innerHTML=rows.map(r=>{ const u=r.latest_dart_url||dartUrl(r.latest_rcept_no); const dilution=r.rights_post_dilution_pct!=null?'유증 '+pct(r.rights_post_dilution_pct):(r.cb_potential_dilution_pct!=null?'CB '+pct(r.cb_potential_dilution_pct):'-'); const issue=r.cb_conversion_price!=null?'CB '+price(r.cb_conversion_price):(r.rights_offer_price!=null?price(r.rights_offer_price):'-'); const verified=(r.terms_verified===true||String(r.terms_verified).toLowerCase()==='true'); return '<tr><td><b>'+safe(r.company)+'</b><div class="mini">'+safe(r.ticker)+'</div></td><td><span class="badge '+safe(r.state)+'">'+safe(r.state)+'</span></td><td>'+safe(r.market_setup_score??r.opportunity_score??'-')+'</td><td>'+safe(r.risk_score??'-')+'</td><td>'+price(r.latest_close)+'</td><td>'+won(r.avg_trading_value_20d_krw)+'</td><td>'+safe(dilution)+'</td><td>'+safe(issue)+'</td><td>'+price(r.cb_reset_floor)+'</td><td>'+safe(r.cb_payment_date||'-')+'</td><td class="terms">'+useText(r)+'</td><td class="'+(verified?'verified':'unverified')+'">'+(verified?'확인':'미확인')+'</td><td>'+(u?'<a target="_blank" rel="noopener" href="'+safe(u)+'">'+safe(r.latest_relevant_filing||'DART 원문')+'</a>':safe(r.latest_relevant_filing||'-'))+'</td><td>'+safe(r.reasons||'-')+'</td></tr>'; }).join(''); const p=board.filter(r=>r.state==='WATCH'||(r.state==='RESEARCH'&&Number(r.avg_trading_value_20d_krw)>=1e9)).sort((a,b)=>Number(b.market_setup_score??0)-Number(a.market_setup_score??0)).slice(0,5); $('priority').innerHTML=p.length?p.map(r=>'<div class="candidate"><b>'+safe(r.company)+'</b> <span class="badge '+safe(r.state)+'">'+safe(r.state)+'</span><div class="mini">희석 '+safe(r.rights_post_dilution_pct??r.cb_potential_dilution_pct??'-')+'% · 거래대금 '+won(r.avg_trading_value_20d_krw)+'</div></div>').join(''):'<div class="muted">현재 우선 상세분석 후보 없음</div>'; }
function renderFilings(){ const q=$('fq').value.trim().toLowerCase(); const rows=filings.filter(r=>!q||String(r.company||'').toLowerCase().includes(q)||String(r.report_nm||'').toLowerCase().includes(q)); $('fc').textContent=rows.length+'건'; $('fbody').innerHTML=rows.map(r=>{const u=dartUrl(r.rcept_no);return '<tr><td>'+safe(r.company)+'</td><td>'+safe(r.ticker)+'</td><td>'+safe(r.report_nm)+'</td><td>'+safe(r.rcept_dt)+'</td><td>'+(u?'<a target="_blank" rel="noopener" href="'+safe(u)+'">원문</a>':'-')+'</td></tr>';}).join(''); }
window.addEventListener('DOMContentLoaded',load);